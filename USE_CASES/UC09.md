# Use Case 9: Export and Import Simulation Data

* [x] Create `src/post_processing/export/touchstone_export.py` for .s1p-.s4p Touchstone format
* [x] Implement HDF5 export for field data with hierarchical organization and compression
* [x] Create `src/post_processing/export/plot_export.py` for PDF/PNG/SVG plot exports
* [x] Implement Touchstone and HDF5 import with validation of imported data integrity
* [x] Write tests in `tests/integration/test_data_io.py` (25 tests)

## CHARACTERISTIC INFORMATION

* Goal in Context: Persist simulation results to standard file formats for sharing, archival, and comparison with other tools.
* Scope: Touchstone S-parameter export, HDF5 field data export, plot export (PDF/PNG/SVG), data import from both formats.
* Level: File I/O operation; extensions handle batch export and partial imports.
* Preconditions: Simulation results exist in memory (S-parameters, fields, metrics).
* Success End Condition: Data is written to or read from disk in valid format with metadata intact.
* Failed End Condition: I/O error or file corruption detected; `SolverError` raised with diagnostics.
* Primary Actor: User or automated pipeline managing simulation data.
* Trigger: User requests export/import via CLI, config file, or API call.

### MAIN SUCCESS SCENARIO

1. System receives export request with format (Touchstone/HDF5/PDF) and output path.
2. For Touchstone: S-parameter matrix is formatted per IEC 61746-3; frequency vector stored.
3. For HDF5: Field data is organized hierarchically (near_field, far_field, geometry) with compression.
4. For plots: Matplotlib figures are rendered at user-specified resolution and layout.
5. File metadata (date, version, simulation parameters) is included in exported files.
6. System validates written file by performing a read-back and comparing data integrity.
7. User receives confirmation with file path and size.

### EXTENSIONS

2a. Multi-port Touchstone (>4 ports) : System writes .sNp format; warns about compatibility.
3a. Partial import : Load only specific datasets from large HDF5 files.

---

## IMPLEMENTATION STATUS

**Completed: 5/5 subtasks** (All export/import functionality implemented and tested)

Implemented:
- `src/post_processing/export/touchstone_export.py` with TouchstoneExporter (supports .s1p-.s4p)
- `src/post_processing/export/touchstone_export.py` with TouchstoneImporter (validation support)
- `src/utils/data_io.py` with HDF5Exporter and HDF5Importer (hierarchical organization, compression)
- `src/post_processing/export/plot_export.py` with PlotExporter (PDF/PNG/SVG output)
- `tests/integration/test_data_io.py` (25 tests covering export, import, round-trips, error handling)

---

## VERIFICATION

```bash
# Export S-parameters to Touchstone
python -c "from src.post_processing.export.touchstone_export import TouchstoneExporter; e = TouchstoneExporter(2)"

# Export field data to HDF5
python -c "from src.utils.data_io import HDF5Exporter; e = HDF5Exporter()"

# Generate plots
python -c "from src.post_processing.export.plot_export import PlotExporter; p = PlotExporter('/tmp')"
```
